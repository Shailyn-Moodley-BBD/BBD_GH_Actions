name: Build Pipeline

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  build:
    run-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    
    - name: 
